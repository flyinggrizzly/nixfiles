if &compatible
  set nocompatible
end

let g:has_async = v:version >= 800 || has('nvim')

call plug#begin('~/.vim/bundle')

" Define bundles via Github repos


Plug 'styrmis/molokai'


Plug 'styrmis/greplace', { 'branch':'allow-blank-filename-list' }

call plug#end()
