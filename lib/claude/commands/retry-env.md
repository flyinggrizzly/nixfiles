Perform the task described by #ARGUMENT. Bear in mind that the dev environment is being refreshed, so if you hit dep or
DB issues, **wait and retry**. Do not skip testing/validation steps just because you can't do something like run the
test env.

If you've retried 5 times, stop and ask the user for directions.
